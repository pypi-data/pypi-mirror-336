[project]
name = "rotate-mob"
version = "0.1.1"
description = "A CLI for mob programming rotations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
authors = [{name = "Nitsan Avni", email = "nitsanav@gmail.com"}]
license = {text = "MIT"}

[project.scripts]
rotate = "rotate.main:main"

[tool.uv]
package = true

[tool.uv.sources]
rotate = { workspace = true }

[project.urls]
Homepage = "https://github.com/nitsanavni/rotate"
