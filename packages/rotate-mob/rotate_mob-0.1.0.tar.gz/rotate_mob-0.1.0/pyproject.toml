[project]
name = "rotate-mob"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
authors = [{name = "Nitsan Avni", email = "nitsanav@gmail.com"}]
license = {text = "MIT"}

[project.scripts]
rotate = "rotate.main:main"

[tool.uv]
package = true

[tool.uv.sources]
rotate = { workspace = true }

[project.urls]
Homepage = "https://github.com/nitsanavni/rotate"
