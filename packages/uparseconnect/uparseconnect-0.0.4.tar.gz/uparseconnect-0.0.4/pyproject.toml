[tool.poetry]
name = "uparseconnect"
version = "0.0.4"
description = "python client library for uparse"
authors = ["Hangxing Wei"]
license = "Apache-2.0 license"
packages = [{ include = "uparse_client" }]

[tool.poetry.dependencies]
python = "^3.10"
pillow = "^10.3.0"
httpx = "^0.27.0"
pydantic = "^2.7.4"
aiofiles = "^24.1.0"
loguru = "^0.7.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
