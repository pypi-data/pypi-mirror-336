[tool.poetry]
name = "puw-gregorio"
packages = [
    { include = "puw-gregorio" },
]
version = "0.1.0"
description = "Safety placeholder"
authors = ["Nostradamus team  <nostradamus@prima.it>"]

[tool.poetry.dependencies]
python = ">=3.9,<4.0"

[tool.poetry.dev-dependencies]
pytest = "^7.4.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
