[project]
name = "torch-rolx"
version = "0.1.0"
description = ""
authors = [
    {name = "Command M"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]
package = {include = ["torch_rolx"]}


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
