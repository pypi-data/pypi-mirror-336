[project]
name = "hotpod-cli"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.8",
    "setuptools>=78.0.2",
]

[project.scripts]
hotpod = "hotpod_cli.cli:cli"
