[project]
name = "lerum"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
lerum = "lerum:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
