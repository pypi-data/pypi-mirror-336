[project]
name = "new-project-floris"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Floris vanderFlier", email = "floris.vanderflier@iff.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.5",
    "ruff>=0.11.2",
]

[project.scripts]
new-project-floris = "new_project_floris:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[pytest]
addopts = "--doctest-modules"

