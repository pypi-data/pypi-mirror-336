[project]
name = "qualmeuip"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
qualmeuip = "qualmeuip.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
