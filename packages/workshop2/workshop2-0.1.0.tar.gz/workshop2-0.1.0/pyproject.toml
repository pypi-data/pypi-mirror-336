[project]
name = "workshop2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.5",
    "ruff>=0.11.2",
]

[project.scripts]
workshop2 = "workshop2:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[pytest]
addopts = "--doctest-modules"
