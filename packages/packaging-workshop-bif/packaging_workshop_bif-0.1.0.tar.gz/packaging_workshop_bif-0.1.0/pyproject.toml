[project]
name = "packaging-workshop-bif"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "noord087", email = "ben.noordijk@wur.nl" }
]
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.3.5",
    "ruff>=0.11.2",
]

[project.scripts]
packaging-workshop-bif = "packaging_workshop_bif:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
