[project]
name = "AISuperServer"
version = "0.1.99"
description = "Es un experimento simple para crear una interfaz simple y rápida de server de Inferencia en Local"
authors = [
    { name = "Rivera.ai/Fredy Rivera", email = "riveraaai200678@gmail.com" }
]

readme = "README.md"

requires-python = ">=3.8"
dependencies = [
    "Flask",
    "flask_cors",
    "waitress",
    "psutil",
    "diffusers",
    "fastapi",
    "uvicorn",
    "ollama"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["AISuperServer"]