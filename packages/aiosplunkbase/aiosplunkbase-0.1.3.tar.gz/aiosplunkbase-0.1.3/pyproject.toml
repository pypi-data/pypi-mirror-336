[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aiosplunkbase"
version = "0.1.3"
description = "Async Python client for Splunkbase"
authors = [{ name = "Cameron Schmidt" }]
dependencies = [
    "httpx>=0.28.1",
    "packaging>=24.2"
]
requires-python = ">=3.9"
readme = "README.md"
license = { text = "MIT" }
