def tuple_sort(item) -> object:
    """
    Returns the first element of a tuple.

    ## Parameters

    - `item`: The tuple to get the first element of.

    ## Returns

    The first element of the tuple.

    ## Example

    ```python
    tuple_sort((1, 2))  # 1
    ```
    """
    return item[0]
