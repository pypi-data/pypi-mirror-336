
[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "monika-pytest_framework"  # Replace with your package name
version = "0.1.0"           # Replace with your version
description = "pytest framework"


[tool.setuptools]
packages = [
    "setup_common",
    "utils"
]

