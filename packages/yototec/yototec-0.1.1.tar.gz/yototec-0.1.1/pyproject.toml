[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yototec"
version = "0.1.1"
description = "A Python client for interacting with the Yototec Market API"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
  { name="Edward Chen", email="rchen@yototec.com" }
]
keywords = ["yototec", "api", "client"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]
dependencies = [
    "requests"
]
requires-python = ">=3.7"

[project.urls]
"Homepage" = "https://github.com/Yototec/client_lightweight"
"Source Code" = "https://github.com/Yototec/client_lightweight"
"Tracker" = "https://github.com/Yototec/client_lightweight/issues"
