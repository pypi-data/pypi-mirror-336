[project]
name = "test-packaging-mnda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Reint Alberda van Ekenstein", email = "reint.alberdavanekenstein@wur.nl" }
]
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.5",
    "ruff>=0.11.2",
]

[project.scripts]
test-packaging-mnda = "test_packaging_mnda:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[pytest]
addopts = '--doctest-modules'
