[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "missdat"
version = "0.1.7"
description = "A package for assessing, diagnosing, and estimating missing data"
authors = [{name = "Drew E. Winters, PhD.", email = "drewEwinters@gmail.com"}]
license = {text = "MIT"}
readme = "README.md"
dependencies = [
    "numpy",
    "pandas",
    "scipy"
]

[project.urls]
Homepage = "https://github.com/drewwint/missdat"

